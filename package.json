{
  "name": "biaoji-website",
  "version": "1.0.2",
  "description": "号码标记清除服务网站",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist/blog dist/docs dist/images && cp index.html dist/ && cp favicon.ico dist/ && cp sitemap.xml dist/ && cp robots.txt dist/ && cp _headers dist/ && cp _redirects dist/ && cp blog/*.html dist/blog/ && cp docs/*.html dist/docs/ && cp images/* dist/images/ && echo \"Build complete\""
  },
  "keywords": [],
  "license": "MIT"
}